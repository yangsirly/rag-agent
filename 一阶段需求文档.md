## 第一阶段需求

### 目标

用户能够注册、登录并使用聊天功能；编辑者能够管理知识库及其文档。

聊天暂不调用真实模型。用户发送消息后，系统保存消息并返回固定模板消息；相同发送请求因超时或网络错误而重试时，不得重复生成消息。

### 核心用例

客户：

1. 使用邮箱注册；
2. 登录、退出登录；
3. 创建新会话；
4. 查看自己的会话列表；
5. 打开历史会话并查看消息；
6. 在会话中发送消息；
7. 收到系统生成的模板回复；
8. 修改会话标题；
9. 删除自己的会话。

编辑者：

1. 完成客户的所有操作；
2. 创建知识库；
3. 查看自己创建或被授权的知识库；
4. 修改知识库；
5. 删除自己创建的知识库；
6. 在知识库中创建文档；
7. 修改和删除文档；
8. 授权其他编辑者管理知识库；
9. 取消其他编辑者的授权。

为了控制规模，也可以把第 8、9 项推迟到第一阶段后半部分，最开始只实现“创建者管理自己的知识库”。

### 非目标

第一阶段明确不做：

- 调用真实大模型；
- RAG 和向量检索；
- 文档解析；
- 文档上传及对象存储；
- Redis；
- 消息队列；
- 多 Agent；
- 知识库内容参与回答；
- 管理员后台；
- 第三方登录；
- 找回密码；
- 短信验证码真实发送。

第一阶段固定只开放邮箱注册接口。`phone` 字段可作为后续能力预留，但本阶段不接收手机号，也不使用固定验证码模拟手机号归属；新增手机号注册时必须重新补充验证码与号码规范化契约。

## 二、工业级非功能需求（增补 2026-08 高并发重构）

> 本章为一次性工业级重构新增，原有功能需求保持不变。所有新增约束均可测试、可压测验证。

### 2.1 性能与容量目标（SLO）

| 场景 | 目标 | 验收方式 |
| --- | --- | --- |
| 注册/登录 | p99 < 300ms @ 100 QPS，错误率 < 0.1% | k6 混合压测 |
| 发送消息（模板阶段） | p99 < 500ms @ 200 QPS | k6 200 VU 持续 30s |
| 历史消息查询 | p99 < 200ms | k6 |
| 单实例容量 | 支撑 1000 在线用户，Hikari 20 连接，Tomcat 200 线程 | 监控 + 压测 |

### 2.2 并发与一致性契约

- 注册：邮箱规范化后唯一，最终仲裁为 uk_users_email；并发重复注册必有一个 409
- 发送消息：同一 conversation_id + client_message_id 全局只产生一对消息
  - 相同 content 重试 -> 200 返回原 id，库中不新增
  - 不同 content 复用 -> 409
  - 并发双发靠 uk_messages 兜底，Redis SET NX 仅 fast-path
  - 验收：50 线程同 key 压测仅 2 行
- 删除会话：会话与消息双侧软删除（`deleted_at` + 批量 UPDATE，每批独立事务），避免 CASCADE 大事务；行保留以维持外键完整性，再次删除返回 404
- 列表/历史：page*size < 1000，深分页 400，后续游标

### 2.3 限流与安全

- 限流：注册/登录 IP 10/min，发送消息 user 20/min；超限 429 + Retry-After + X-RateLimit-*
- 防刷：登录连续失败 5 次锁定 15 分钟（Redis 计数 + users.lock_until）
- 认证：JWT 新增 jti，logout 写入 Redis 黑名单至过期；Filter 先查黑名单

### 2.4 可观测与部署

- 可观测：traceId + 耗时，暴露 health/metrics/prometheus
- 部署：docker-compose 新增 redis:7；Hikari 20/5/3000，Tomcat 200

### 2.5 非目标调整

- 原不做 Redis/MQ 调整为：为高并发引入 Redis（限流/幂等/黑名单/登录失败计数）；RabbitMQ 通道推迟到接入真实模型时再实现完整 PENDING→DONE 状态机，不引入空壳脚手架。不引入分库分表/K8s


## 三、完善业务规则

### 注册

建议先确定：

- 第一阶段用户必须使用邮箱注册；
- 邮箱规范化后唯一；
- 邮箱统一转为小写并去除首尾空格；
- 手机号字段仅为后续预留，第一阶段请求不得提交；
- 密码长度建议为 8～64 个字符；
- 密码不能明文存储；
- 使用 BCrypt 或 Argon2 哈希；
- 第一阶段注册用户默认为 `CUSTOMER`；
- 不允许用户自己指定为 `EDITOR`；
- 编辑者身份通过数据库初始化或管理员脚本产生。

必须考虑并发注册：

```
两个请求同时使用同一邮箱注册
```

仅在业务层“先查询邮箱是否存在”是不够的，数据库仍然需要唯一约束。

### 登录

需要补充：

- 登录失败统一提示“账号或密码错误”；
- 不暴露账号是否存在；
- 被禁用用户不能登录；
- 登录态有明确有效期；
- 采用双 Token：Access JWT 有效期 15 分钟，Refresh 为固定 7 天的高熵随机凭证；Refresh 仅保存 SHA-256 哈希并在每次刷新时严格轮换；
- 退出登录撤销当前设备 Refresh 会话并将当前 Access `jti` 加入黑名单；其他设备会话不受影响；
- 未登录访问受保护接口返回 `401`；
- 无权操作资源返回 `403` 或统一返回 `404`；
- 提供 `GET /me`，让前端在 HttpOnly Cookie 场景下于页面刷新后恢复用户和角色状态；
- 前端普通接口收到 `401 UNAUTHORIZED` 时 single-flight 调用 `/refresh`，成功后仅重试原请求一次；刷新失败才清理本地登录状态并跳转登录页。

注意：

> “跳转登录界面”是前端行为。后端的责任是返回明确的 `401 Unauthorized`。

### 会话和消息

需要明确：

- 会话必须属于一个用户；
- 用户只能查看、修改、删除自己的会话；
- 消息必须属于一个会话；
- 用户不能向别人的会话发送消息；
- 消息发送后应持久化用户消息；
- 随后生成并持久化模板回复；
- 客户端为一次发送生成 `client_message_id`，网络重试必须复用该值；
- 同一会话内相同 `client_message_id` 只能产生一条用户消息和一条模板回复；
- 历史消息按创建时间排序；
- 历史消息第 0 页返回最新一页，每页内部仍按时间正序展示；
- 删除会话时，其消息一起删除；
- 空消息和超长消息不能发送；
- 第一阶段不允许修改或删除单条消息。

建议限制：

```
会话标题：1～100 字
消息内容：1～10000 字
```

模板回复例如：

```
已收到你的问题。本系统当前处于第一阶段，暂未接入真实模型。
```

### 知识库

需要明确：

- 只有 `EDITOR` 可以创建知识库；
- 名称必填；
- 同一创建者下知识库名称唯一；
- 创建者拥有修改、删除和授权权限；
- 被授权编辑者可以修改知识库和文档；
- 被授权编辑者不能删除知识库或继续授权其他人；
- 删除知识库时，其文档一起删除；
- 客户不能访问知识库管理接口。

建议第一阶段的权限定义：

| 操作           | 创建者 | 被授权编辑者 | 客户 |
| -------------- | ------ | ------------ | ---- |
| 查看知识库     | 是     | 是           | 否   |
| 修改知识库     | 是     | 是           | 否   |
| 创建、修改文档 | 是     | 是           | 否   |
| 删除文档       | 是     | 是           | 否   |
| 授权编辑者     | 是     | 否           | 否   |
| 删除知识库     | 是     | 否           | 否   |

### 文档

第一阶段不做真实文件上传，那么“文档”应暂时定义为可编辑的文本内容：

```
标题
摘要
文本内容
所属知识库
创建者
创建和修改时间
```

否则只有“文档简述”，这个实体没有真正可管理的内容。

## 四、建议的数据模型

### 用户 `users`

```
id
email                  第一阶段业务必填，唯一
phone                  可空，后续手机号注册预留
password_hash          非空
role                   CUSTOMER / EDITOR
status                 ACTIVE / DISABLED
created_at
updated_at
```

约束：

```
数据库保留 email 和 phone 至少一个不为空的扩展约束
第一阶段业务层额外要求 email 非空、phone 为空
email 唯一
phone 唯一
```

不建议使用模糊的“权限等级”数字，例如 `0、1、2`。使用含义明确的枚举更容易维护。

### 会话 `conversations`

```
id
user_id
title
created_at
updated_at
```

第一阶段固定使用物理删除，会话与其消息在同一事务中删除，并通过外键约束避免孤儿消息。首次删除成功后再次删除同一 ID，应表现为资源不存在。

索引：

```
(user_id, updated_at)
```

用于查询当前用户的历史会话列表。

### 消息 `messages`

```
id
conversation_id
role                   USER / ASSISTANT
content
client_message_id      USER 消息必填，客户端生成的 UUID
reply_to_message_id    ASSISTANT 消息必填，指向对应 USER 消息
created_at
```

索引：

```
(conversation_id, created_at, id)
```

幂等与消息配对约束：

```text
UNIQUE (conversation_id, client_message_id)
UNIQUE (reply_to_message_id)
USER 消息：client_message_id 非空，reply_to_message_id 为空
ASSISTANT 消息：client_message_id 为空，reply_to_message_id 非空
```

唯一约束是并发幂等的最终保证，不能只在业务层“先查询是否存在”。相同 `client_message_id` 携带不同内容时返回冲突，不能静默复用第一次内容。

第一阶段不要把 `role` 和用户角色混淆：

```
用户角色：CUSTOMER / EDITOR
消息角色：USER / ASSISTANT
```

### 知识库 `knowledge_bases`

```
id
creator_id
name
description
created_at
updated_at
```

不建议存储：

```
manager_id
document_count
last_modified_by
```

原因如下：

- 一个知识库可能有多个管理者，单个 `manager_id` 无法表达；
- `document_count` 可以查询得到，直接存储容易与真实文档数不一致；
- `last_modified_by` 第一阶段不是必需字段。

如果后面确实有列表性能问题，再增加文档数量冗余，并设计一致性方案。

### 知识库授权 `knowledge_base_members`

```
id
knowledge_base_id
user_id
permission             EDIT
granted_by
created_at
```

唯一约束：

```
(knowledge_base_id, user_id)
```

还需要业务层验证：

- 被授权用户必须存在；
- 被授权用户必须是 `EDITOR`；
- 授权接口使用规范化邮箱定位目标用户，成员表仍存稳定的 `user_id`；
- 不能授权知识库创建者自己；
- 不能重复授权；
- 只有创建者可以授权；
- 创建者不需要再次出现在授权表中。

如果想让第一批功能更小，可以先不建这张表，只支持创建者管理；完成基础知识库功能后再通过数据库迁移加入授权。

### 文档 `documents`

```
id
knowledge_base_id
creator_id
title
summary
content
created_at
updated_at
```

通常不需要再存 `manager_id`。权限由所属知识库决定：

```
能管理知识库
→ 能管理该知识库下的文档
```

这种权限继承比文档独立授权简单很多。

## 五、需要补充的异常情况

当前列出的异常还不够。建议加入：

### 注册与登录

- 邮箱为空、格式非法或已注册；
- 密码错误；
- 用户不存在；
- 用户被禁用；
- 登录凭证缺失；
- 登录凭证伪造；
- 登录状态过期；
- 并发注册相同账号。

### 会话和消息

- 会话不存在；
- 访问他人会话；
- 消息为空；
- 消息超过长度限制；
- 用户使用相同 `client_message_id` 重试同一条消息；
- 相同 `client_message_id` 被用于不同消息内容；
- 用户消息保存成功，但模板回复保存失败；
- 会话已删除后继续发送消息。

### 知识库和文档

- 客户调用编辑者接口；
- 知识库不存在；
- 编辑者没有知识库权限；
- 知识库名称为空或重复；
- 文档不属于指定知识库；
- 文档标题或内容为空；
- 删除知识库后继续修改其文档；
- 授权不存在的用户；
- 授权客户而不是编辑者；
- 被授权用户重复授权；
- 被取消授权后继续操作。

## 六、消息发送需要考虑事务

发送消息看起来简单，但很适合训练事务设计。

一次发送包含两个数据库写入：

```
校验会话所有权和 client_message_id
→ 保存 USER 消息
→ 生成模板内容
→ 保存指向 USER 消息的 ASSISTANT 消息
→ 更新会话 updated_at
```

你需要决定：

### 方案 A：一个事务完成

任意一步失败，全部回滚；数据库唯一约束保证并发重试只会形成一组消息。

优点：

- 不会只有用户消息而没有模板回复；
- 超时重试不会重复生成用户消息和模板回复；
- 第一阶段简单。

缺点：

- 将来接入耗时的大模型调用时，不应该把数据库事务保持到模型回复结束。

第一阶段建议采用方案 A，但需要写一份技术决策：

> 当前模板回复没有外部调用，因此使用单事务保证两条消息一致；接入真实模型后会改为异步状态或拆分事务。

这就是很好的面试素材。

## 七、将第一阶段拆成五个小里程碑

不要一次让 AI 实现所有功能。

### 里程碑 1：用户注册

完成：

- 用户表和数据库迁移；
- 邮箱注册；
- 密码哈希；
- 邮箱唯一；
- 参数校验；
- 注册集成测试。

暂时不实现登录。

### 里程碑 2：登录与认证

完成：

- 登录；
- 身份凭证；
- 退出；
- 登录过期；
- 用户状态检查；
- 当前用户接口 `GET /me`；
- 未登录访问返回 401；
- 前端能够根据 401 跳转。

如果前后端分离，需要明确使用哪种认证方案。第一版可以选择：

- Spring Security；
- JWT Access Token（15 分钟）与随机 Refresh Token（固定 7 天）；
- 两个 Token 均放在 HttpOnly Cookie，Refresh 只存哈希并严格轮换；
- Refresh 重放撤销当前设备会话，Access `jti` 黑名单用于即时吊销。

或者选择 Spring Session JDBC。关键不是哪个更“高级”，而是你能解释选择和边界。

### 里程碑 3：聊天闭环

完成：

```
创建会话
→ 发送用户消息
→ 保存消息
→ 生成模板回复
→ 保存模板回复
→ 查询历史消息
```

发送接口同时完成 `client_message_id` 幂等：相同 ID 重试返回原消息对，不重复写入。

这是第一个真正的业务垂直切片。

### 里程碑 4：会话管理和权限

完成：

- 历史会话列表；
- 修改标题；
- 删除会话；
- 只能操作自己的会话；
- 资源不存在和访问他人资源的测试。

### 里程碑 5：知识库与文档

先实现创建者权限：

- 编辑者创建知识库；
- 编辑者管理自己的知识库；
- 文档文本 CRUD；
- 客户不能调用管理接口；
- 其他编辑者不能修改。

完成后再增加知识库授权表。授权接口以规范化邮箱接收目标编辑者，后端解析为 `user_id` 后持久化；授权路由未实现前不暴露占位接口，前端隐藏入口。

## 八、修改后的验收标准

### 用户流程

```
Given 一个未注册用户
When 使用合法邮箱和密码注册并登录
Then 可以进入系统
And 页面刷新后可以通过 GET /me 恢复用户和角色状态
```

### 聊天流程

```
Given 用户已经登录
When 创建会话并发送问题
Then 数据库保存一条 USER 消息
And 数据库保存一条 ASSISTANT 模板消息
And 接口返回模板回复
And 重新打开会话后仍能看到两条消息

When 使用相同 client_message_id 重试发送
Then 返回与首次相同的 USER 和 ASSISTANT 消息 ID
And 数据库消息数量不增加
```

### 权限隔离

```
Given 用户 A 和用户 B 都已经登录
When 用户 B 查询、修改或删除用户 A 的会话
Then 操作失败
And 用户 A 的数据没有变化
```

### 知识库

```
Given 一个 EDITOR 用户
When 创建知识库和文本型文档
Then 可以查看和修改该知识库及文档
```

### 角色权限

```
Given 一个 CUSTOMER 用户
When 调用知识库创建或修改接口
Then 返回 403
```

### 授权

```
Given 编辑者 A 创建知识库并授权编辑者 B
When 编辑者 B 修改知识库中的文档
Then 修改成功

When 未授权编辑者 C 尝试修改
Then 操作失败
```

### 工程验收

- 数据库由 Flyway 初始化；
- 注册、认证、会话、消息、权限具有集成测试；
- 密码和 Token 不出现在日志中；
- 使用统一错误响应；
- 所有数据查询包含所有权或授权条件；
- 消息幂等依赖数据库唯一约束，并有并发或重复请求测试；
- Cookie 认证的外部部署必须启用 CSRF 防护或经过测试的等价防护，CORS 不允许通配 Origin 携带凭证；
- 项目可以通过 Docker Compose 启动；
- API 文档可查看；
- 测试可以在干净环境中执行。
